---
# Source: splunk-connect-for-kubernetes/charts/splunk-kubernetes-objects/templates/clusterRole.yaml
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: cluster-audit-logging
rules:
  - apiGroups: [""]
    resources:
    - pods
    - events
    - namespaces
    verbs: ["get", "list", "watch"]
  - apiGroups: ["templates.gatekeeper.sh", "config.gatekeeper.sh", "status.gatekeeper.sh", "constraints.gatekeeper.sh"]
    resources:
    - "*"
    verbs: ["get", "list", "watch"]
